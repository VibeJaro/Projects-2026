{
  "name": "projects-2026",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Mission Control 2026 - static dashboard for project planning and progress logging.",
  "scripts": {
    "prepare-env": "node ./scripts/write-env.js",
    "dev": "npm run prepare-env && python -m http.server 3000",
    "start": "npm run prepare-env && python -m http.server 3000",
    "build": "npm run prepare-env && echo \"Static build - nothing to compile\"",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
